# =============================================================================
# Streamlit Cloud — runtime dependencies
# =============================================================================
#
# This file is intentionally minimal. Streamlit Cloud reads requirements.txt
# from the repository root to build the app environment.
#
# It contains ONLY what the Streamlit dashboard needs at runtime:
#   - Streamlit itself
#   - DuckDB (read-only queries against the bundled .duckdb file)
#   - Pandas, Plotly (data wrangling and charts)
#   - Anthropic (AI Coach page — bring-your-own-key)
#   - Requests (Open-Meteo weather API)
#   - Loguru, Pydantic (used by ai_engine/ and ingestion/config.py)
#   - python-dotenv (DEFAULT_CITY fallback for local development)
#
# For local development (ingestion pipeline, dbt, Airflow, tests):
#   pip install -r requirements-dev.txt
#
# Version notes:
#   - streamlit==1.45.0  — 1.46+ has a serialisation bug with DuckDB 1.4.x
#     (Arrow field id: 100 error). Pinned until upstream fix is confirmed.
#   - duckdb==1.4.4      — must match the version used to write the .duckdb file
# =============================================================================

streamlit==1.45.0
duckdb==1.4.4
pandas>=2.0.0
plotly>=5.0.0
requests>=2.31.0
python-dotenv>=1.0.0
anthropic>=0.25.0
loguru>=0.7.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
